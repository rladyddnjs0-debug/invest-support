# invest-support

데이터 기반 시장 분석/버블 탐지/AI 리서치를 결합한 Streamlit 투자 의사결정 지원 대시보드 (`app.py` 진입점, `modules/` 핵심 로직).

## 개발 워크플로우

서버(앱) 관련 작업 요청 시 다음 순서로 진행한다:

1. `main` 브랜치 기준 최신 상태에서 `feat/*` 브랜치 생성
2. 해당 브랜치로 git worktree 생성 후 그 워크트리에서 작업
   - 신규 로직을 추가하는 작업이면 관련 문서(`docs/*.md`, README 항목 등)를 함께 작성한다.
   - 기존 로직을 제거하는 작업이면 해당 로직을 설명하던 문서도 함께 삭제(또는 갱신)한다.
3. 작업 완료 후 `main`으로 PR 생성
4. PR 머지
5. 워크트리 제거 및 작업 브랜치(`feat/*`) 삭제 (로컬/원격 모두)

메인 워크트리에서 직접 작업하거나 커밋하지 않는다.